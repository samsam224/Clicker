using Microsoft.VisualStudio.TestTools.UnitTesting;
using System.Windows;
using System;
using Cliker;

namespace Testing
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void TestMethod1()
        {
            //int a = 5;
            //int b = 5;
            //Assert.IsTrue(a + b == 10);

            //var page = new MainWindow();

            //Assert.IsTrue(page.AB(5, 5));
            //Assert.IsFalse(page.ClickMethod(5, 5));
            //Assert.IsTrue(page.ClickMethod(5, 5));
        }
    }
}
